Q.Promt for the maximum and minimum temperature readings on a particular day, accept those readings as integer, and culculate, and display to the screen the average temperature.
```psedocode
START
  PROMPT "Please key in max and min temperatures"
  GET min_temp, max_temp AS INTEGER
  avg_tmp = (mix_temp + max_temp) / 2
  DISPLAY avg_tmp
END
```